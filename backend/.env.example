PORT=4000

PGHOST=localhost
PGPORT=5432
PGDATABASE=bi_platform
PGUSER=postgres
PGPASSWORD=changeme

# 32-byte (64 hex char) key used to encrypt data_connections.encrypted_password
# at rest (AES-256-GCM). Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CONNECTION_ENCRYPTION_KEY=
